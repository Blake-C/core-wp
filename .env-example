WORDPRESS_DB_HOST=db
WORDPRESS_DB_USER=root
WORDPRESS_DB_PASSWORD=root
WORDPRESS_DB_NAME=core_wp
MYSQL_ROOT_PASSWORD=root
MYSQL_PASSWORD=root
WORDPRESS_TABLE_PREFIX=wp_
# Set to "debug" to enable XDebug step debugging (adds PHP overhead — turn off when not in use).
XDEBUG_MODE=off
